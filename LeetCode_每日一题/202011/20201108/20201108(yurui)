class Solution {
public:
    int maxProfit(vector<int>& prices) {
    int minprice=1e9;
    int maxpro=0;
    int n=prices.size();
    for(int i=0;i<n;i++)
    {
        minprice=min(minprice,prices[i]);
        maxpro=max(maxpro,prices[i]-minprice);
    }
    return maxpro;
    }
};
